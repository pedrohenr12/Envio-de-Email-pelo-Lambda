# 📄 Projeto AWS Lambda - Envio de E-mails com Amazon SES

## 🎯 Função da Lambda

Esta função Lambda foi desenvolvida para realizar o envio automático de e-mails utilizando o serviço Amazon SES (Simple Email Service).  
Ela recebe informações de destinatário, assunto e corpo da mensagem para enviar e-mails de forma automatizada.  
É útil para aplicações que necessitam de notificações, confirmações ou alertas por e-mail.

---

## 📥 Entrada Esperada

A função espera receber um objeto JSON com os seguintes campos:

- `to`: endereço de e-mail do destinatário (deve estar verificado no Amazon SES, enquanto a conta estiver no modo sandbox)
- `subject`: assunto do e-mail
- `body`: texto do corpo da mensagem

---

## 📤 Saída Esperada

A função retorna uma resposta JSON indicando sucesso ou falha no envio:

- Em caso de sucesso, retorna um status `200` com uma mensagem confirmando o envio e o ID da mensagem.
- Em caso de erro, retorna um status `500` com uma descrição do problema encontrado.

---

## 📦 Dependências Externas

A função utiliza a biblioteca `boto3`, que é a SDK oficial da AWS para Python e já está disponível por padrão no ambiente Lambda.

---

## ⚙️ Instruções para Execução

1. **Criar a função Lambda** no console da AWS, escolhendo Python como linguagem.
2. **Configurar a função** para enviar e-mails via Amazon SES.
3. **Verificar os endereços de e-mail** no console do Amazon SES para que possam ser usados (requisito do modo sandbox).
4. **Ajustar as permissões** da função Lambda:
   - Atribuir à role associada a política `AmazonSESFullAccess` para garantir que a função tenha permissão para enviar e-mails.
5. **Testar a função** usando eventos JSON que contenham os campos `to`, `subject` e `body`.

---

## 🧪 Como Testar a Função

- No console da AWS Lambda, criar um evento de teste simulando a estrutura JSON esperada.
- Executar a função e verificar o retorno.
- Confirmar que o e-mail chegou na caixa do destinatário.
- Caso a função apresente erros, verificar os logs no CloudWatch para diagnosticar o problema.

---

## 🐞 Como Depurar a Função

- Acesse os logs no CloudWatch para visualizar mensagens de erro e detalhes da execução.
- Erros comuns:
  - **AccessDenied**: geralmente indica falta de permissões na role IAM da Lambda.
  - **MessageRejected**: significa que o e-mail remetente ou destinatário não está verificado no Amazon SES.
  - Problemas com formato dos dados de entrada ou erro na configuração da função.

---

## 🔗 Integração com GitHub

O projeto foi versionado em um repositório público no GitHub, contendo:

- Código-fonte da função Lambda.
- Documentação completa para uso e configuração.
- Instruções detalhadas para testes e depuração.

---

## ✅ Resultado Final

A função Lambda está funcionando corretamente, enviando e-mails via Amazon SES com as permissões e verificações necessárias.  
O projeto é fácil de configurar, seguro e pode ser utilizado para diversas aplicações que demandem envio automático de e-mails.

